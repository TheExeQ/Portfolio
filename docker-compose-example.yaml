version: '3'
services:
  portfolio-website:
    image: portfolio-website-image
    container_name: portfolio-website
    restart: unless-stopped
    ports:
      - '3000:3000'
    networks:
      - nginx-router
      
networks:
  nginx-router:
    external: true